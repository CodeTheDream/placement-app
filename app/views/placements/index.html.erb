
<div class='card bg-light col-5 rounded mt-2 shadow-sm ml-3'>
    
  <h3 class='mt-2 text-center'>All Placements</h3>
  <h4 class='mt-2 text-center'>Click on a placement or licensee to view details</h4>
    
  <div class='card-body'>
    <% @licensees.order(:facility_name).each do |l| %>
      <ul class='list-group'>
        <li class='list-group-item-info mt-2 shadow'>
          <%= link_to l.facility_name, licensee_path(l.id), 
          class: 'border-bottom border-primary font-weight-bold' %>
        </li>
        <li class='list-group-item mt-2 shadow'>
          <% l.placements.each do |p| %>
            <%= link_to p.name, placement_path(p.id), class: 
            'border-bottom border-primary font-weight-bold' %>
            <br>
            <%= p.address %>
            <br>
            <%= p.city %>, <%= p.state %> <%= p.zip %>
            <br>
            <%= link_to 'Add to Cart', add_carts_path(placement_id: 
            p.id), method: :post, class: 'btn btn-primary mt-2' %>
            <br>
          <% end %>
        </li>
      </ul>     
    <% end %>
  </div>
</div> 

)
  
