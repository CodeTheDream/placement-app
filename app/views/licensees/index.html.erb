<div class='mt-4 mb-4'>
    <h3>Licensees</h3>
</div>

<br><br>

<% @licensees.each do |l| %>
<div class='card bg-light col-3 rounded mb-0 text-center'>
    <div class='card-body'>
        <%= link_to l.contact_name, licensee_path(l.id) %>
    </div>
</div>
<% end %>